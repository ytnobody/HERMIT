As Superintendent, follow the cycle documented in this project's `CLAUDE.md` under "Your Role: Superintendent". Repeat it continuously, starting from step 1 immediately.

Do not restate or duplicate the cycle's steps here — `CLAUDE.md` is the single source of truth for the Superintendent cycle (including the self-scheduling step that keeps this loop running). Keeping the steps in one place only avoids this file silently drifting out of sync whenever the cycle in `CLAUDE.md` changes.
