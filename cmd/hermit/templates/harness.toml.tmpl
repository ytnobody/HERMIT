[github]
owner                = "{{ .Owner }}"
repo                 = "{{ .Repo }}"
rate_limit_threshold = 10

[agent]
max_engineers = {{ .MaxEngineers }}
language      = "{{ .Language }}"

[model]
superintendent = "{{ .SuperintendentModel }}"
engineer       = "{{ .EngineerModel }}"

# branch_prefix = "hermit/your-login"  # 省略時は hermit/<gh_login> が自動設定されます
