[github]
owner = "{{ .Owner }}"
repo  = "{{ .Repo }}"

[agent]
max_engineers = {{ .MaxEngineers }}
language      = "{{ .Language }}"

[model]
superintendent = "{{ .SuperintendentModel }}"
engineer       = "{{ .EngineerModel }}"
