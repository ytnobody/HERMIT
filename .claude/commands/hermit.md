As Superintendent, follow the dispatch procedure documented in this project's `CLAUDE.md` under "Your Role: Superintendent". Run **only** the "Foreground dispatch" steps inline in this session: ensure the recurring cron trigger exists, spawn the background Superintendent subagent (Agent tool with `run_in_background: true`) that executes one pass of the "Background cycle", and return control to the prompt immediately. Never execute the Superintendent cycle itself in the foreground — the whole point is that the user keeps a usable prompt while the cycle runs in the background.

Do not restate or duplicate the cycle's steps here — `CLAUDE.md` is the single source of truth for both the foreground dispatch and the background cycle (including the self-scheduling step that keeps this loop running). Keeping the steps in one place only avoids this file silently drifting out of sync whenever the cycle in `CLAUDE.md` changes.
